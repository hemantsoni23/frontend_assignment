// src/components/RoleMessage.jsx

const RoleMessage = () => (
  <div className="flex items-center justify-center h-screen">
    <p className="text-gray-600 text-lg">Waiting for admin to assign your role...</p>
  </div>
);

export default RoleMessage;
